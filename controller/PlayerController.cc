#include "PlayerController.h"

const Controller::ActionAndTarget PlayerController::getAction(const std::shared_ptr<Character> &character, const std::vector<ActionAndRange> &actions,
                                                  const std::shared_ptr<State> &state) {
    return {};
}
